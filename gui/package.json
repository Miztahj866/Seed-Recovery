{
  "name": "seed-recovery-tool-gui",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop GUI for the local, offline BIP39 seed phrase recovery tool",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0"
  }
}
